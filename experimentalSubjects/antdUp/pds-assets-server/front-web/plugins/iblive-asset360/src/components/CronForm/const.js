export const dayAry = ['日', '一', '二', '三', '四', '五', '六'];
export const monthAry = Array(12)
  .fill(0)
  .map((_, index) => index + 1);
export const dateAry = Array(31)
  .fill(0)
  .map((_, index) => index + 1);
export const timeAry = Array(60)
  .fill(0)
  .map((_, index) => index);
export const hourAry = Array(24)
  .fill(0)
  .map((_, index) => index);
